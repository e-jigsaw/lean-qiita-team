{
  "name": "lean-qiita-team",
  "version": "0.0.0",
  "description": "lean your qiita team",
  "scripts": {
    "build": "rm -rf build && mkdir build && lsc -cbo build src/*.ls && lson -o build src/*.lson"
  },
  "author": "jigsaw (http://jgs.me)",
  "license": "MIT",
  "devDependencies": {
    "LiveScript": "^1.3.1",
    "lson-cli": "0.0.1"
  }
}
